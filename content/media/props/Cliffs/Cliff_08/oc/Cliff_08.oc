Cliff_08 extends G3PropsInstance
{
	CustomFadeAlpha = 1.0;
	Walkable = true; // whenever you change the 'walkable' property of a prop, make sure to correctly move it between 'prop_packages.oo' and 'walkable_prop_packages.oo'. This ensures that all walkable props are preloaded correctly before a game starts.

	Destructible = false;
	BlocksBuildings = true;
	
	static var Description = ActorDesc
	{
		Visual = "Cliff_08";
	};
	EditorName = "Cliff08";
	EditorIcon = "media/UserInterface/PropIcons/Cliff08.png";
	EditorCategory = -1; // needs to be pickable so we define it as -1
}